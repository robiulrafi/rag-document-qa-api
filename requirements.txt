# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11

# --- LLM + RAG (local, $0 — no API keys required) ---
langchain>=0.3.14
langchain-core>=0.3.29
langchain-ollama>=0.2.2
langchain-chroma>=0.2.0            # vector store wrapper
langchain-community>=0.3.0        # BM25Retriever, PyPDFLoader
langchain-text-splitters>=0.3.0   # RecursiveCharacterTextSplitter (fallback splitter)

# --- Vector store + embeddings ---
chromadb>=0.5.0
pypdf>=5.0                         # PDF loading (PyPDFLoader backend)

# --- Hybrid retrieval (BM25 keyword search) ---
rank-bm25>=0.2.2

# --- Cross-encoder reranking ---
sentence-transformers>=3.0        # CrossEncoder (ms-marco-MiniLM-L-6-v2)

# --- Agentic RAG ---
langgraph>=0.2.0                  # self-correcting graph

# --- Testing ---
pytest>=8.3
httpx>=0.28